⚠️ DEPRECATED TASK — DO NOT USE
你现在接手一个正在进行中的毕业设计工程项目。你的目标不是一次性“做完全部”，而是以最稳的方式，接管后续“阶段四前端开发 + 阶段三剩余小接口补全”工作。你必须按增量方式推进，每次只做一个短任务，每个短任务做完后必须停止，向我汇报结果并等待我确认后才能继续。

====================
一、工作方式（强制遵守）
====================

1. 先 Ask 后 Code
- 每个新阶段开始前，先用 Ask 模式输出：
  - 你理解的目标
  - 你将执行的 1 个最小子任务
  - 会改哪些文件
  - 不会改哪些文件
  - 验收标准
- 只有我明确批准后，才进入 Code 模式执行

2. 一次只做一个短任务
- 绝对不要一次性并行做多个功能
- 每次只做一个功能或一个小接口
- 每次结束后，代码必须保持可运行、可测试、可回滚

3. 每步必须停下来等我验收
- 你每完成一个短任务后，必须停止
- 向我输出：
  - 改了什么
  - 改了哪些文件
  - 如何验证
  - 当前结果截图/页面预期
  - 下一步建议
- 未经我确认，不得继续下一个任务

4. 严格最小权限
- 不允许删除大块代码
- 不允许大规模重构目录
- 不允许修改生产密钥、删除数据库、重建项目
- 不允许擅自改环境变量
- 不允许新增重型依赖，除非先 Ask 说明理由并获得我批准

5. 不允许擅自修改冻结接口
当前后端接口已冻结，前端必须服从，不允许重命名：
- POST /upload_generated
- GET /queue
- GET /next_image
- POST /mark_shown

6. 必须保留现有工程成果
当前项目已有：
- Supabase 已配置
- 表与 bucket 已建立
- Flask 后端主干已跑通
- upload_generated 已验证成功
你只能在这个基础上增量修改，不允许推倒重来

7. 必须建立进度文档
如果仓库根目录没有 AGENTS.md / CODEX_PROGRESS.md，请先创建：
- AGENTS.md：项目规则
- CODEX_PROGRESS.md：进度、已完成任务、待办、风险、如何恢复上下文
每个任务结束后更新 CODEX_PROGRESS.md

8. 必须测试
每次代码修改后，必须运行对应的最小验证：
- 后端接口：最小 API 测试
- 前端页面：本地可打开、关键按钮可点击
- 联调：最小 happy path
未测试通过，不得宣称完成

9. 必须可回滚
每完成一个短任务，必须建议一个清晰 commit message。
如果我允许，你再执行 git commit；未经允许不要自动推远程。

====================
二、项目背景（必须理解）
====================

这是一个“手机网页 + LED 大屏装置 + 算法视觉”的互动艺术系统。

核心体验流程：
1. 用户在手机网页上传图片
2. 前端本地将图片转化为“色盲图谱 / Ishihara 风格”图像
3. 用户点击“传输到 LED”
4. 后端接收生成图，写入数据库与展示队列
5. 图像在 LED 大屏轮播展示
6. 用户进入留言页，写下感受
7. 用户再进入打印弹窗
8. 打印弹窗展示文案与打印编号
9. 用户进入支付流程（当前可先做 sandbox / 占位）
10. 用户可下载电子版图像

作品逻辑：
生成 → 展示 → 表达 → 留存 → 物化

对应关系：
- LED 展示 = 被看见
- 留言 = 表达
- 打印 = 带走

====================
三、当前已知工程事实（必须遵守）
====================

1. 数据库与存储
Supabase 已建立以下结构：
- submissions 表
- display_queue 表
- original-images bucket
- generated-images bucket

2. submissions 负责保存：
- upload_order
- uploaded_at
- original_image_url
- generated_image_url
- symbol
- display_status
- message
- printed
- print_count
- print_code
- payment_status
- created_at

3. display_queue 负责保存：
- submission_id
- generated_image_url
- queue_order
- status
- created_at
- shown_at

4. 展示队列规则
- 点击“传输到LED”后创建 display_queue
- 展示页读取 status='pending'
- 按 created_at 升序
- 若时间相同按 queue_order 升序
- 取第一条
- 展示开始后改为 showing
- 展示完成后改为 shown

5. 打印编号规则
- print_code 从 001 开始递增
- 只有点击“打印”时才生成
- upload_order 和 print_code 彼此独立，不可混用

6. 留言规则（非常关键）
留言发生在“上传到LED之后”，打印之前：
- 上传图片
- 生成图谱
- 点击“传输到LED”
- 创建 submission + queue
- 进入留言界面
- 用户填写留言
- 写入 submissions.message
- 进入打印流程

7. 支付规则
- payment_status: unpaid / paid
- 当前先按 Stripe Sandbox / 占位流程设计
- 不要求你立即打通真实收款，优先保证流程壳子完整且可替换

====================
四、当前系统现状（必须沿用）
====================

后端已有基础能力：
- /health 正常
- /upload_generated 已能接收前端生成图
- 能上传 generated image 到 Supabase Storage
- 能写入 submissions
- 能写入 display_queue

当前网络测试结论：
- 本地 Wi-Fi 对 Supabase HTTPS 可能不稳定
- 手机热点测试成功
结论：主干链路已打通，不要误判为代码逻辑失败

====================
五、你现在要接管的工作范围
====================

你只接管以下两部分：

A. 阶段四：前端正式手机版开发
B. 阶段三：剩余小接口补全

你不负责：
- 重建 Supabase
- 推翻现有 Flask 主干
- 修改数据库总体结构
- 擅自接入真实支付并要求我立刻提供全部商户配置
- 重命名冻结接口

====================
六、整体任务拆解（必须按顺序执行）
====================

你必须按下面顺序，一个任务一个任务推进。每做完一步必须停下来等我确认。

任务 0：建立规则与进度文件
目标：
- 创建 / 更新 AGENTS.md
- 创建 / 更新 CODEX_PROGRESS.md
完成标准：
- AGENTS.md 明确写清：
  - 冻结接口
  - 最小权限
  - 每步先确认
  - 不允许大改目录
- CODEX_PROGRESS.md 记录当前已完成事项与待办
完成后停下，等我验收

任务 1：梳理现有代码结构并输出前端实施计划（Ask only）
目标：
- 不改代码
- 阅读现有 backend / frontend（如果 frontend 为空则说明）
- 输出“前端骨架实施计划”
完成标准：
- 给出页面列表
- 给出文件结构建议
- 给出接口映射关系
- 给出最小任务拆分
完成后停下，等我确认

任务 2：搭建前端骨架
目标：
创建手机端优先的前端骨架，只包含：
- OpeningScreen
- UploadScreen
- GenerateScreen
- ResultActionScreen
- MessageScreen
- PrintModal
- PaymentPlaceholderScreen
要求：
- 参考Apple designresources设计规范设计干净、极简、可运行的 UI
- 所有 API 调用集中在单独模块
完成标准：
- 网址能通过手机端打开
- 页面结构清楚，功能完全
- 有路由或页面切换机制
- 暂不要求所有按钮完全联通
完成后停下，等我验收视觉基础是否合格

任务 3：接通上传与传输链路
目标：
- 前端上传图片
- 前端本地生成图谱（可先使用现有生成逻辑）
- 点击“传输到 LED”调用 POST /upload_generated
- 成功后进入留言页
完成标准：
- 在手机端或本地浏览器可完整走通：
  上传 → 生成 → 传输 → 后端成功返回 → 跳到留言页
- 必须验证 submissions 和 display_queue 写入正常
完成后停下，等我验收流程

任务 4：补后端留言接口
目标：
新增一个最小接口，例如：
- POST /submit_message
作用：
- 接收 submission_id + message
- 写入 submissions.message
完成标准：
- 留言页提交成功
- 数据库中 message 正确更新
完成后停下，等我验收

任务 5：接通留言页
目标：
- 留言页具备输入框和提交按钮
- 提交成功后进入打印弹窗
完成标准：
- 流程：
  传输成功 → 留言页 → 提交 → 打印弹窗
完成后停下，等我验收

任务 6：补打印逻辑
目标：
新增最小打印接口，例如：
- POST /create_print_order
功能：
- 读取当前最大 print_code
- 生成下一个三位编号（001, 002, 003...）
- 更新 submissions.printed=true
- 更新 submissions.print_count
- 更新 submissions.print_code
完成标准：
- 打印弹窗能显示正确 print_code
- 数据库正确更新
完成后停下，等我验收

任务 7：接通打印弹窗
目标：
在打印弹窗中显示如下文案逻辑：
“代号XXX 归序者，请完成相纸使用的序值缴纳，以继续通行。”
要求：
- print_code 动态显示
- 具备“取消 / 缴纳”操作
完成标准：
- 文案和编号显示正确
- 点击“缴纳”进入支付占位页
完成后停下，等我验收文案与视觉

任务 8：支付占位页
目标：
先做支付流程壳子，不强行打通真实支付
页面应包含：
- 微信支付
- 支付宝
- 确认缴纳
并预留后续真实接入位置
完成标准：
- 点击“缴纳”能进入支付页
- 点击确认后可把 payment_status 更新为 paid（可先通过后端占位接口）
完成后停下，等我验收

任务 9：下载功能
目标：
- 在结果页支持下载生成图 PNG
完成标准：
- 下载按钮可用
完成后停下，等我验收

任务 10：移动端 UI 精修（第一轮）
目标：
在不改接口与主流程的前提下，提升视觉与体验。
风格方向：
- 手机端优先
- 极简 / 高级 / 具有仪式感
- 不做过度复杂动画
- 强调叙事感与展览感
完成标准：
- 至少完成一版“可审美验收”的 UI
- 你必须先给我预览说明，再停下来等我决定是否返工
完成后停下，等我验收

任务 11：展示页联调（如需要）
目标：
- 确保 display 页面可读取 /next_image
- 展示结束后调用 /mark_shown
完成标准：
- 队列状态可从 pending → showing → shown
完成后停下，等我验收

====================
七、前端设计要求（必须遵守）
====================

前端最终是移动端网页，不是桌面网页。
所有交互必须优先考虑手机单手使用。

设计倾向：
A. 极简、手绘、插画感、高级感
B. 沉浸式、带叙事感、轻动态

但当前执行策略是：
- 先做 A 为主的极简骨架
- 再用轻量动态补叙事感
- 不要一开始就做复杂沉浸动画
- 先保证流程可用，再做美术强化

前端原则：
- 每完成一个页面或流程节点，都必须立刻停下来让我验收
- 如果我说“不合格”，你必须只修改指定问题，不得顺手大改其他部分

====================
八、必须遵守的代码与安全边界
====================

1. 不得打印、回显或提交任何密钥
- 不要在输出里复述 .env 中的值
- 不要把 secrets 写入前端
- 不要把 service_role key 暴露到客户端

2. 不得修改数据库结构
除非我明确授权

3. 不得修改以下冻结接口名
- POST /upload_generated
- GET /queue
- GET /next_image
- POST /mark_shown

4. 新增接口必须先 Ask 说明
例如：
- POST /submit_message
- POST /create_print_order
- POST /update_payment_status
都必须先说明再做

5. 不得擅自安装大型新依赖
如需安装，必须先 Ask 说明：
- 为什么必须装
- 影响哪些文件
- 如何回滚

6. 每一步必须给出最小测试方式
例如：
- 如何本地打开页面
- 点哪个按钮
- 预期会看到什么
- 数据库会出现什么变化

====================
九、每一步你的输出格式（强制）
====================

每次进入 Ask 阶段，你必须按以下格式输出：

[当前任务]
一句话说明当前只做哪一个最小任务

[目标]
这一步完成后会发生什么

[将修改的文件]
列出具体文件路径

[不会修改的内容]
列出明确不碰的文件或模块

[验证方式]
我如何检查这一步是否成功

[完成后停止]
明确写：完成后我会停止，等待你确认

每次进入 Code 阶段完成后，你必须按以下格式输出：

[已完成]
这一步做了什么

[修改文件]
具体改了哪些文件

[验证结果]
实际测试了什么，结果是什么

[你现在可以如何验收]
给我最短的操作步骤

[建议下一个任务]
只允许给出一个下一个任务建议

====================
十、Done When（总完成标准）
====================

只有当以下全部完成，项目才算本轮接管任务完成：

1. 手机端前端骨架完成
2. 上传 → 生成 → 传输 → 留言 → 打印弹窗 → 支付占位 → 下载 全流程可走通
3. 后端补齐最小留言 / 打印 / 支付状态接口
4. 与 Supabase submissions / display_queue 正确联动
5. 界面经过至少一轮人工审美验收
6. 你输出了更新后的 AGENTS.md 和 CODEX_PROGRESS.md
7. 每一步都有测试依据
8. 每一步都经过我确认后才继续

====================
十一、现在立刻开始的第一步
====================

现在先进入 Ask 模式，只做“任务 0：建立规则与进度文件”。

不要写代码，不要改接口，不要开始做前端。
先阅读仓库，输出：
- 你理解到的当前项目结构
- 你准备新增/更新的 AGENTS.md 和 CODEX_PROGRESS.md 的内容提纲
- 你会改哪些文件
- 你不会改哪些文件
- 我将如何验收这一步

然后停止，等待我确认。